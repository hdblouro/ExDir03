// Faça um script que inicie a contagem em 8 e vá até 120, inclusive, 
// com incremento de 12 em 12, mostrando o valor da variável a cada volta no console.

for (let i: number = 8; i <= 120; i += 12) {
    console.log(i);
}
